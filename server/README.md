Server (Node + Express) — Phase 1

- Run:
  cd server
  npm install
  npm run dev

- Health routes:
  GET / -> { message: 'Agriculture Smart Farming API - Phase 1' }
  GET /health -> { status: 'ok' }
